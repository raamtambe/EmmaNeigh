name: Build EmmaNeigh

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          cd desktop-app/python
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build Python executables
        run: |
          cd desktop-app/python
          pyinstaller --onefile --name signature_packets signature_packets.py
          pyinstaller --onefile --name execution_version execution_version.py
          pyinstaller --onefile --name checklist_parser checklist_parser.py
          pyinstaller --onefile --name incumbency_parser incumbency_parser.py
          pyinstaller --onefile --name sigblock_workflow sigblock_workflow.py
          pyinstaller --onefile --name document_collator document_collator.py
          pyinstaller --onefile --name email_csv_parser email_csv_parser.py
          pyinstaller --onefile --name time_tracker time_tracker.py
          pyinstaller --onefile --name checklist_updater checklist_updater.py
          pyinstaller --onefile --name punchlist_generator punchlist_generator.py
          pyinstaller --onefile --name email_nl_search email_nl_search.py
          pyinstaller --onefile --name packet_shell_generator packet_shell_generator.py

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install npm dependencies
        run: |
          cd desktop-app
          npm install

      - name: Prepare resources
        run: |
          mkdir -p desktop-app/resources/win
          cp desktop-app/python/dist/signature_packets.exe desktop-app/resources/win/
          cp desktop-app/python/dist/execution_version.exe desktop-app/resources/win/
          cp desktop-app/python/dist/checklist_parser.exe desktop-app/resources/win/
          cp desktop-app/python/dist/incumbency_parser.exe desktop-app/resources/win/
          cp desktop-app/python/dist/sigblock_workflow.exe desktop-app/resources/win/
          cp desktop-app/python/dist/document_collator.exe desktop-app/resources/win/
          cp desktop-app/python/dist/email_csv_parser.exe desktop-app/resources/win/
          cp desktop-app/python/dist/time_tracker.exe desktop-app/resources/win/
          cp desktop-app/python/dist/checklist_updater.exe desktop-app/resources/win/
          cp desktop-app/python/dist/punchlist_generator.exe desktop-app/resources/win/
          cp desktop-app/python/dist/email_nl_search.exe desktop-app/resources/win/
          cp desktop-app/python/dist/packet_shell_generator.exe desktop-app/resources/win/
        shell: bash

      - name: Build Electron app (NSIS + Portable)
        run: |
          cd desktop-app
          npx electron-builder --win --publish never

      - name: Upload to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            desktop-app/dist/EmmaNeigh-Setup.exe
            desktop-app/dist/EmmaNeigh-Portable.exe
            desktop-app/dist/latest.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-mac:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Python dependencies
        run: |
          cd desktop-app/python
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build Python executables
        run: |
          cd desktop-app/python
          pyinstaller --onefile --name signature_packets signature_packets.py
          pyinstaller --onefile --name execution_version execution_version.py
          pyinstaller --onefile --name checklist_parser checklist_parser.py
          pyinstaller --onefile --name incumbency_parser incumbency_parser.py
          pyinstaller --onefile --name sigblock_workflow sigblock_workflow.py
          pyinstaller --onefile --name document_collator document_collator.py
          pyinstaller --onefile --name email_csv_parser email_csv_parser.py
          pyinstaller --onefile --name time_tracker time_tracker.py
          pyinstaller --onefile --name checklist_updater checklist_updater.py
          pyinstaller --onefile --name punchlist_generator punchlist_generator.py
          pyinstaller --onefile --name email_nl_search email_nl_search.py
          pyinstaller --onefile --name packet_shell_generator packet_shell_generator.py

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install npm dependencies
        run: |
          cd desktop-app
          npm install

      - name: Prepare resources
        run: |
          mkdir -p desktop-app/resources/mac
          cp desktop-app/python/dist/signature_packets desktop-app/resources/mac/
          cp desktop-app/python/dist/execution_version desktop-app/resources/mac/
          cp desktop-app/python/dist/checklist_parser desktop-app/resources/mac/
          cp desktop-app/python/dist/incumbency_parser desktop-app/resources/mac/
          cp desktop-app/python/dist/sigblock_workflow desktop-app/resources/mac/
          cp desktop-app/python/dist/document_collator desktop-app/resources/mac/
          cp desktop-app/python/dist/email_csv_parser desktop-app/resources/mac/
          cp desktop-app/python/dist/time_tracker desktop-app/resources/mac/
          cp desktop-app/python/dist/checklist_updater desktop-app/resources/mac/
          cp desktop-app/python/dist/punchlist_generator desktop-app/resources/mac/
          cp desktop-app/python/dist/email_nl_search desktop-app/resources/mac/
          cp desktop-app/python/dist/packet_shell_generator desktop-app/resources/mac/
          chmod +x desktop-app/resources/mac/signature_packets
          chmod +x desktop-app/resources/mac/execution_version
          chmod +x desktop-app/resources/mac/checklist_parser
          chmod +x desktop-app/resources/mac/incumbency_parser
          chmod +x desktop-app/resources/mac/sigblock_workflow
          chmod +x desktop-app/resources/mac/document_collator
          chmod +x desktop-app/resources/mac/email_csv_parser
          chmod +x desktop-app/resources/mac/time_tracker
          chmod +x desktop-app/resources/mac/checklist_updater
          chmod +x desktop-app/resources/mac/punchlist_generator
          chmod +x desktop-app/resources/mac/email_nl_search
          chmod +x desktop-app/resources/mac/packet_shell_generator

      - name: Build Electron app
        run: |
          cd desktop-app
          npx electron-builder --mac --publish never

      - name: Upload to Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            desktop-app/dist/*.dmg
            desktop-app/dist/latest-mac.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
